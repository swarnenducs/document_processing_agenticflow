# Local monorepo install for plain pip (workspace packages are NOT on PyPI).
# Prefer:  uv sync
#
# Usage:
#   python -m pip install -U pip
#   pip install -r requirements-workspace.txt

-e ./document-processing-mcp
-e ./voice_enable_mcp
-e ./central-agentic-flow
-e ./ip_api
-e ./UI
pytest>=8.0.0
ruff>=0.9.0
