# File changes — 2026-08-28 04:14:22 (IST)

Inventory of working-tree changes, grouped by module.
Status letters: `A` added, `M` modified, `R` renamed, `AM` added then further modified, `MM` staged plus unstaged edits.

This folder is a snapshot list only. It is **not** a git commit.

Not listed (never commit): `.env`, `UI/config/ui_runtime.json`, `AGENTS.md`.
