Share this folder with the UI team.

  UI_API_INTEGRATION_GUIDE.txt
      Contract for Angular: base URL, CORS, headers, every route, document
      job flow (multipart + WebSocket), voice HITL, chat, admin PyJWT
      (POST /api/v1/admin/token, HS256, no certificate).

  ADMIN_PYJWT_QA.txt
      Spoken Q&A for PyJWT: why, flow, claims, secret, no cert, status
      codes, code map, interview questions. Use this to answer follow-ups.

  API_ROUTE_CHECK_AND_AZURE_SQL.txt
      Route probe results (2026-09-01) and whether Azure SQL config exists.
