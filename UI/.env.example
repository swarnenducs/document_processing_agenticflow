# UI (Gradio) — copy to .env in this folder (standalone git).
# Display-only: no LLM keys, no SQL, no Blob. Talks to ip_api only.
#
#   cp .env.example .env
#   python -m ui_app.ui.gradio_app

API_BASE_URL=http://127.0.0.1:8000
GRADIO_HOST=127.0.0.1
GRADIO_PORT=7860
